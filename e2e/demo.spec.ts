import {test,expect} from '@playwright/test';
const png=Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+A8AAQUBAScY42YAAAAASUVORK5CYII=','base64');
test('login → vehicle/media → content/video → HOT handoff',async({page})=>{await page.goto('/login');await page.getByRole('button',{name:'Demo hesaba gir'}).click();if(await page.getByLabel('Galeri adı').isVisible().catch(()=>false)){await page.getByLabel('Galeri adı').fill('Growth Motors');await page.getByRole('button',{name:'Satış kokpitini aç'}).click()}await page.getByRole('button',{name:/Yeni Araç/}).first().click();await page.getByRole('button',{name:'Araç oluştur'}).click();await expect(page.getByRole('heading',{name:'BMW 320i M Sport'})).toBeVisible();await page.getByLabel('Araç fotoğrafları').setInputFiles([{name:'front.png',mimeType:'image/png',buffer:png},{name:'rear.png',mimeType:'image/png',buffer:png}]);await expect(page.getByAltText('Yüklenen araç görünümü')).toHaveCount(2);await page.getByRole('button',{name:/AI ile Sat/}).click();await expect(page.getByRole('heading',{name:'AI Satış İçerikleri'})).toBeVisible();await page.getByRole('button',{name:'Video oluştur'}).click();await expect(page.getByRole('status')).toContainText('MP4 değildir');await page.getByRole('button',{name:'Demo WhatsApp mesajı'}).click();await expect(page.getByText('/100 HOT')).toBeVisible();await expect(page.getByText(/2020 passat/i)).toBeVisible();await expect(page.getByText(/Ziyaret: Bugün/)).toBeVisible();await page.getByRole('button',{name:'Mesajlar'}).click();await expect(page.getByText('HUMAN_REQUIRED')).toBeVisible();await page.getByRole('button',{name:'Devral'}).click();await expect(page.getByText('HUMAN_ACTIVE')).toBeVisible();});
