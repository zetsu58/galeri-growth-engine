"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const [busy,setBusy]=useState(false),router=useRouter();async function login(){setBusy(true);await fetch("/api/auth/demo",{method:"POST"});router.push("/");router.refresh()}return <main className="center"><section className="authCard"><div className="logo">GALERİ <span>GROWTH</span></div><p className="eyebrow">SATIŞ ZEKÂSI PLATFORMU</p><h1>Aracı ekle.<br/>AI müşterisini bulsun.</h1><p>Demo ortamı dış servislere ihtiyaç duymaz.</p><button disabled={busy} onClick={login}>{busy?"Giriş yapılıyor…":"Demo hesaba gir"}</button><small>demo@galeri.local • Şifre gerekmez</small></section></main>}
