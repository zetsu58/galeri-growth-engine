"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Onboarding(){const [name,setName]=useState("Growth Motors"),router=useRouter();return <main className="center"><form className="authCard" onSubmit={async e=>{e.preventDefault();const r=await fetch('/api/onboarding',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name})});if(r.ok){router.push('/');router.refresh()}}}><p className="eyebrow">HOŞ GELDİNİZ</p><h1>Galerinizi oluşturun</h1><label>Galeri adı<input value={name} onChange={e=>setName(e.target.value)} required/></label><button>Satış kokpitini aç</button></form></main>}
