import path from "node:path";const allowed={"image/jpeg":[".jpg",".jpeg"],"image/png":[".png"],"image/webp":[".webp"]} as const;export const MAX_IMAGE_BYTES=5*1024*1024;
export function validateImage(name:string,type:string,bytes:Uint8Array){if(bytes.length===0||bytes.length>MAX_IMAGE_BYTES)throw new Error("INVALID_UPLOAD");const ext=path.extname(name).toLowerCase();const extensions=allowed[type as keyof typeof allowed];if(!extensions?.includes(ext as never))throw new Error("INVALID_UPLOAD");const jpeg=bytes[0]===0xff&&bytes[1]===0xd8&&bytes[2]===0xff;const png=bytes.length>=24&&bytes.slice(0,8).every((x,i)=>x===[137,80,78,71,13,10,26,10][i])&&new TextDecoder().decode(bytes.slice(12,16))==="IHDR";const webp=new TextDecoder().decode(bytes.slice(0,4))==="RIFF"&&new TextDecoder().decode(bytes.slice(8,12))==="WEBP";if(!((type==="image/jpeg"&&jpeg)||(type==="image/png"&&png)||(type==="image/webp"&&webp)))throw new Error("INVALID_UPLOAD");return {ext:extensions[0],mime:type}}
