import {z} from "zod";
export const vehicleInput=z.object({brand:z.string().trim().min(1).max(50),model:z.string().trim().min(1).max(60),trim:z.string().trim().min(1).max(80),year:z.coerce.number().int().min(1900).max(2100),mileage:z.coerce.number().int().nonnegative().max(5000000),price:z.coerce.number().positive(),minimumPrice:z.coerce.number().positive().optional(),transmission:z.string().min(1).max(30),fuel:z.string().min(1).max(30),color:z.string().min(1).max(40),condition:z.string().min(1).max(60),damageInfo:z.string().max(1000),exchangeAvailable:z.boolean(),financingInfo:z.string().max(1000),notes:z.string().max(2000),status:z.enum(["draft","active","reserved","sold","archived"])}).strict();
