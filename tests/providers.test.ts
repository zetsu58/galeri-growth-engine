import {describe,expect,it} from 'vitest';import {MockAIProvider,salesContentSchema} from '../src/lib/ai/provider';import {ingestMockMessage} from '../src/lib/messaging/mock';
describe('offline providers',()=>{it('generates validated factual content',async()=>{const x=await new MockAIProvider().generateSalesContent({brand:'BMW',model:'320i',trim:'M Sport',year:2022,mileage:48000,price:2150000,transmission:'Otomatik',fuel:'Benzin'});expect(salesContentSchema.safeParse(x).success).toBe(true);expect(x.listingTitle).toContain('BMW');});it('is idempotent per tenant',()=>{const x={eventId:'e1',dealershipId:'00000000-0000-4000-8000-000000000001',from:'905551112233',text:'BMW bugün gelebilirim, takas'};expect(ingestMockMessage(x).duplicate).toBe(false);expect(ingestMockMessage(x).duplicate).toBe(true);});it('rejects malformed messages',()=>expect(()=>ingestMockMessage({})).toThrow());});
