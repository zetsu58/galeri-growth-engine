import {describe,it,expect} from "vitest";import type {Database} from "../src/lib/domain";
function scoped(db:Database,tenant:string){return {vehicles:db.vehicles.filter(x=>x.dealershipId===tenant),leads:db.leads.filter(x=>x.dealershipId===tenant),conversations:db.conversations.filter(c=>db.leads.some(l=>l.dealershipId===tenant&&l.conversationId===c.id))}}
describe("behavioral tenant isolation",()=>it("cannot observe or mutate another tenant's graph",()=>{const db={users:[],dealerships:[],vehicles:[{id:'v2',dealershipId:'d2'}],contents:[],leads:[{id:'l2',dealershipId:'d2',conversationId:'c2'}],conversations:[{id:'c2'}],videoJobs:[],followUps:[],events:[]} as unknown as Database;const one=scoped(db,'d1');expect(one.vehicles).toEqual([]);expect(one.leads).toEqual([]);expect(one.conversations).toEqual([]);}));
