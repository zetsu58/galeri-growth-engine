import {describe,it,expect} from "vitest";import {validateImage,MAX_IMAGE_BYTES} from "../src/lib/security/upload";describe("upload validation",()=>{it("accepts matching PNG magic and extension",()=>expect(validateImage('a.png','image/png',new Uint8Array([137,80,78,71,13,10,26,10,0,0,0,13,73,72,68,82,0,0,0,1,0,0,0,1])).mime).toBe('image/png'));it("rejects MIME spoofing",()=>expect(()=>validateImage('evil.png','image/png',new TextEncoder().encode('MZ executable'))).toThrow());it("rejects traversal/extensions and oversized input",()=>{expect(()=>validateImage('../../x.exe','image/png',new Uint8Array([137,80,78,71,13,10,26,10,0,0,0,13,73,72,68,82,0,0,0,1,0,0,0,1]))).toThrow();expect(()=>validateImage('x.png','image/png',new Uint8Array(MAX_IMAGE_BYTES+1))).toThrow()})});
